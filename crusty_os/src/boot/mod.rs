//! Boot-protocol abstraction layer.
//!
//! The protocol-neutral [`KernelBootInfo`] type (defined in `framework`) is
//! produced by each boot adapter and passed to [`crate::allocator_init`].
//!
//! | Feature            | Protocol       | Entry symbol  | Adapter                  |
//! |--------------------|----------------|---------------|--------------------------|
//! | `boot-multiboot2`  | Multiboot2     | `kernel_main` | `barnacle::entry_point!` |
//! | `boot-limine`      | Limine         | `_start`      | `boot::limine` (below)   |

// ── Compile-time mutual-exclusion guards ──────────────────────────────────────

#[cfg(all(feature = "boot-multiboot2", feature = "boot-limine"))]
compile_error!("boot-multiboot2 and boot-limine are mutually exclusive.");

// ── Protocol modules ──────────────────────────────────────────────────────────

// boot-multiboot2: kernel_main is generated by barnacle::entry_point! in main.rs.

#[cfg(feature = "boot-limine")]
pub mod limine;
